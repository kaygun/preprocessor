# Header

## Subheader

List:

1. One
2. Two
3. Three
   + one
   + two 
   + three

[link](http://google.com)

Here is a simple code block:
``` 
val a = 1
val b = 2

a+b-2
```
Then we have the following
```
val n = 200
(1 to n).filter(_%7 == 0).map(x=>(x/7)*2).toArray
```
Let me see if we can do complicated objects
```
object test {
  val a = 3
  def move(n:Int) = n*n - 3*n + 7
}
```

Let us see if this still works:
```
List(test.a,test.move(20))
```
